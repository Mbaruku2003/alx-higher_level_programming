#!/usr/bin/python3
"""Define a module."""


def is_same_class(obj, a_class):
    """
    Define what the function will return
    we will use type to test wheather object
    and a-class are of same type.
    """
    return type(obj) == a_class
